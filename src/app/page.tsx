/* ONLY WEB MODE = CLASSIC (owner 2026-09-03, branch POPIKA_No_Vector):
   the alternative customer UX is removed — the classic configurator IS
   the site, running on the dream engine (three dreamed labels per brief,
   300dpi TIFF downloads). The /classic route stays as an alias (parity
   captures and old links use it). */
export { default } from "./classic/page";
