# Build outputs (regenerated by node build.js)
dist/
.next/
tsconfig.tsbuildinfo
next-env.d.ts

# Dependencies
node_modules/
package-lock.json
yarn.lock

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Env (secrets live here)
.env
.env.local
.env*.local

# Parity check outputs (regenerable; reference/ and golden/ ARE committed as the spec)
tests/parity/ported/
tests/parity/diff/
tests/parity/failures/
tests/parity/imagegen-e2e.png

# One-off exports
docs/DESIGN.pdf

# Server-side runtime data (art direction config, sessions)
data/
